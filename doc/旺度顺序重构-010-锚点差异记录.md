# 五行打分顺序重构（010）锚点差异记录

**Branch**: `010-reorder-wangdu-scoring` | **日期**: 2026-08-27
**规格**: `specs/010-reorder-wangdu-scoring/`（spec/plan/research/tasks/data-model/contracts 完整）

## 1. 重构内容

`compute_wangdu` 计算顺序重构为 **定性(第1-5步) → 定量(第6-11步)**（spec FR-001，Q1-Q3 用户拍板）：

- **定性（只判定性质与归属）**：
  1. 月令能否合化 → `month_effective_wx`（**单一化神基准**，Q2=A，取代 009 双状态平均）
  2. 月令旺相休囚死（以月令有效五行为基准）
  3. 地支关系判定（书 §9 论处先后，让位维持 009/Q1=B；合化成功的支做藏干重组）
  4. 地支根气保留（`root_preserved`，供通根/从格）
  5. 天干能否合化（紧贴三对，`_stem_he_hua_ok` 原口径）
- **定量（落数值）**：
  6. 五行基础分数 → 7. 地支刑冲破害数值（含合绊减力）→ 8. 计算通根 → 9. 旺相休囚系数 → 10. 天干生克（紧贴三对，**先合-冲再生克**，Q3=A；生克优先级 同性克>异性生>异性克>同性生，Q1=B；天干冲按同性克倍率进度数）→ 11. 总分数
- 下游：geju / dayun / yongshen **沿用**（根气反映刑冲破害后状态）。

**步骤键序列**（14 键，废弃 009 的 static/dynamic_a/dynamic_b/final）：
`month_hua → month_state → branch_rel → branch_root → stem_hua → base_score → branch_effects → tonggen → month_coef → stem_shengke → total → geju → dayun → yongshen`

## 2. 009 → 010 锚点数值差异（test_wangdu.py 3 项重录 + 10 项结构性迁移）

| 命例 | 009 值 | 010 值 | 变化原因 |
|---|---|---|---|
| 乾 壬子癸丑辛酉己亥（金） | 6.56 偏弱 | **4.56 较弱**（乙卯运 2.06 / 丙辰运 5.56） | 月令丑被亥子丑会水 → 月令有效五行=水（单一化神基准，Q2=A）；金对水为**休 0.8**（009 双状态平均 相/休） |
| 乾 乙卯丁亥壬戌壬寅（水） | 1.05 太弱 | **1.44 太弱** | 亥卯半三合化木 → 月令有效五行=木；水对木为**休 0.8**（009 双状态平均） |
| 坤 壬子癸丑乙巳癸未 | 正格身弱 | **从印格**（水 26.9 太旺、印透壬癸，用神水） | 子丑合化水 → 月令有效五行=水（旺 2.0）→ 水太旺从印（009 双状态平均水未达从印） |

**结构性迁移（数值不变，仅步骤键/断言位置变化）**：7 项 009 专属步骤测试（static/dynamic_a/dynamic_b 断言）迁移到 `stem_hua`/`stem_shengke`/`branch_effects`；`test_fix_zi_mao_xing_diao` 刑掉断言迁到 `branch_effects`（第7步）；`test_missing_time_*` 时柱缺失提示迁到 `base_score` 步。

**新增行为（无既有锚点受影响）**：天干相冲（甲庚/乙辛/丙壬/丁癸，紧贴）按同性克倍率（主×0.7/受×0.5）进度数——009 天干冲不进度数，010 新增（research R3/Q3）。

## 3. 契约与前端同步

- `test_charts_api.py` / `test_xiyong.py`：steps 键断言改为 `wangdu.STEP_KEYS`（14 键）。
- `frontend/src/types.ts`：`WangduStep.key` 联合类型更新为 14 键；`StrengthDetail.vue`/`relations.ts` 不改（泛化渲染 / 009 让位口径维持）。
- 前端测试 fixture（`ChartResult.spec.ts`/`StrengthDetail.spec.ts`）旧键更新为 010 键。

## 4. 验收

- 后端 `python -m pytest`：**295 用例全绿**（含 8 项 010 新锚点：月令单基准、合绊不变、定性/定量分离、根气保留、天干冲、贪合忘生克、生克优先级、14 键序列）。
- 前端 `npx vitest run`：**153 用例全绿**。
